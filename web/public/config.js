// Dev placeholder. Empty values make the app fall back to import.meta.env
// (VITE_API_BASE / VITE_TILES_URL / VITE_TILES_KEY from .env). In the container
// this file is overwritten at startup with the real env values.
window.__CONFIG__ = { apiBase: "", tilesUrl: "", tilesKey: "" };
